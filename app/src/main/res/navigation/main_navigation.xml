<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_navigation"
    app:startDestination="@id/fragment_home">

    <fragment
        android:id="@+id/fragment_home"
        android:name="com.solar.movie.presentation.home.HomeFragment"
        android:label="Home"
        tools:layout="@layout/fragment_home">
    </fragment>

    <fragment
        android:id="@+id/fragment_favorite"
        android:name="com.solar.movie.presentation.favorite.FavoriteFragment"
        android:label="Favorite"
        tools:layout="@layout/fragment_favorite">
    </fragment>

    <fragment
        android:id="@+id/fragment_my"
        android:name="com.solar.movie.presentation.my.MyFragment"
        android:label="MyFragment" >
    </fragment>

    <fragment
        android:id="@+id/fragment_movie_detail"
        android:name="com.solar.movie.presentation.movie.detail.MovieDetailFragment"
        android:label="MovieDetailFragment" />
</navigation>